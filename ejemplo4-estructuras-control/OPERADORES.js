/*
- OPERADORES ARITMÉTICOS:
    + SUMA
    - RESTA
    * MULTIPLICACIÓN
    / DIVISIÓN
    % MÓDULO(El resto)
    ++ SUMA 1 A LA VARIABLE ASIGNADA (EJ: edadAlumno1++(si edadAlumno1 fuese 3, ahora es 4))
        variable++ (primero se realiza la operación y después incrementa)
        ++variable (primero incrementa, después realiza la operación)
    -- RESTA 1 A LA VARIABLE ASIGNADA (EJ: edadAlumno1--(si edadAlumno1 fuese 3, ahora es 2))
        variable-- (primero se realiza la operación y después decrementa)
        --variable (primero decrementa, después realiza la operación)
    
- OPERADORES DE COMPARACIÓN Y CONDICIONALES
        > MAYOR QUE
        < MENOR QUE
        >= MAYOR O IGUAL QUE
        <= MENOR O IGUAL QUE
        == IGUALACIÓN (nos dice si ambas variables tienen el mismo valor) (EJ: (2==2)true, (3==2)false)
        != DIFERENTE A (si son distintos dará true, si son diferentes dará true) (EJ: (2!=2)false, (3!=2)true)

        && ES COMO AND, EVALÚA QUE AMBAS CONDICIONES SE CUMPLAN (EJ: edadAlumno>18 && nombreAlumno=="María")ambas se tienen que cumplir para que sea true
        || ES COMO OR, EVALÚA QUE AL MENOS UNA CONDICIÓN SE CUMPLA (si al menos una se cumple va palante, si no se cumple ninguna pues nada)
        ! NEGACIÓN NIEGA LO QUE TENGA DENTRO (EJ: !(true)=false  !(false)=true)

- OPERACIONES DE ASIGNACIÓN
        += USO( operador1+=operador2) EQUIVALE A(operador1 = operador1 + operador2) (EJ: edadAlumno1(18)+=edadAlumno2(20) -> edadAlumno1 = 18 + 20 = 38)
        -= USO( operador1-=operador2) EQUIVALE A(operador1 = operador1 - operador2)
        += USO( operador1*=operador2) EQUIVALE A(operador1 = operador1 * operador2)
        /= USO( operador1/=operador2) EQUIVALE A(operador1 = operador1 / operador2)
        %= USO( operador1%=operador2) EQUIVALE A(operador1 = operador1 % operador2)
*/